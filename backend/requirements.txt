# Backend — IASD Gestão
# Python 3.11+
Django>=5.2,<5.3
djangorestframework>=3.16,<3.17
djangorestframework-simplejwt>=5.5,<5.6
django-cors-headers>=4.4
django-filter>=24.0
Pillow>=11.0
# Servir arquivos estáticos em produção (PythonAnywhere) sem Nginx:
whitenoise>=6.6
# Ler variáveis de ambiente do .env:
python-dotenv>=1.0
# Banco: SQLite (padrão do Django) — não precisa de driver extra.
